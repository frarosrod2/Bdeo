# BDEO Technical Test

Monorepo for BDEO Technical Test

## MongoDB

To start a local MongoDB container with Docker run the following command:

```bash
docker run -d --name mongo-bdeo -p 27017:27017 mongo:latest
```